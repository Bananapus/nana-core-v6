TestSurplusAllowanceOf_Local
├── when a project has the specific surplus configured
│   └── it will return uint256 surplusAllowance
└── when it doesn't have the specific surplus configured
    └── it will return 0
